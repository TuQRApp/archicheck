# Auditoría Fase 1 — Detalle: `Fase 2/Herramientas_CubiCasa5k/` (motor CAD)

> Anexo de detalle. Ver síntesis y priorización en [Auditoria_Fase1_Hallazgos.md](Auditoria_Fase1_Hallazgos.md).
> Generado por agente Explore dedicado (2026-09-21), lectura completa de los archivos listados. Reproducido casi verbatim por trazabilidad — cada afirmación cita archivo:línea.

## 1. `cuerpo_cerrado.py` (1659 líneas)

**Qué hace:** Motor de fusión geométrica determinística para muros/puertas/ventanas desde segmentos vectoriales de PDF. Decide (a) qué par de líneas paralelas es el "ancho real" de un muro (`ancho_por_emparejamiento`), (b) si dos grupos de segmentos son el mismo cuerpo cerrado y deben fusionarse (`cuerpo_cerrado_fusiona`, vía rasterizado+dilatación+componente-conexa con cv2), (c) clasificación de "no-muro" (ventana por línea central, hoja/vano de puerta) con detección explícita de conflictos entre tipologías (`clasificar_no_muro`), (d) reconstrucción de ventanas por jamba (`reconstruir_ventanas_por_jamba`) y (e) corte de muros por ventanas confirmadas post-fusión (`cortar_muros_por_ventanas`, `reasociar_puertas_tras_corte`).

**Hardcodeos:**
- **`cuerpo_cerrado.py:205`** — `def _hay_linea_central_entre(s, c, contexto, centrales_ids, d, tol_centro_px=8):` y uso en **línea 236** (`if abs(d_l - d / 2) > tol_centro_px`). Es un umbral geométrico real (qué tan cerca del punto medio debe caer una línea central para que el par se clasifique como ventana, no muro), en **píxeles fijos**, no en metros vía `catalogo_tipologias.parametro()`. Esto **contradice directamente** el principio que el propio docstring del módulo enuncia en líneas 28-32 ("Parametros (tolerancias) leidos desde catalogo_tipologias.py, no como constantes locales... un valor parametrico debe vivir en UN solo lugar rastreable"). Además, al ser un valor en px puro (no dividido por `mpx`), no escala con la resolución/escala del plano — un plano a otro DPI o escala gráfica distinta tendría una tolerancia real (en metros) distinta sin que nadie lo note.
- **`cuerpo_cerrado.py:691`** — `snap_px = 3` (grilla de snapping de vértices para el remate de esquinas) y **`cuerpo_cerrado.py:870`** — `def _grupo_toca_componente(segmentos, componente, box, paso_px=2):` (paso de muestreo). Ambos son magic numbers en píxeles no catalogados, aunque de naturaleza más técnica/de muestreo que normativa — severidad menor que el caso anterior.
- Confirmé por lectura cruzada completa que el resto de las 12 constantes de tolerancia del módulo (líneas 40-51: `_TOL_MIN_M`, `_TOL_MAX_M`, `_TOL_SIMETRIA_M`, `_TOL_VERTICE_M`, `_ANCHO_MAX_HOJA_CONFIRMADA_M`, `_MARGEN_CONTEXTO_M`, `_TOL_CONECTOR_ESQUINA_M`, `_TOL_JAMBA_M`, `_ANCHO_VENTANA_MIN_M`, `_ANCHO_VENTANA_MAX_M`, `_MAX_SPREAD_VERTICAL_M`, `_TOL_CROSS_M`, más `piso_min_px`/`tol_fusion_pct` en línea 974/1097) sí pasan por `_param()` y sus valores **coinciden exactamente** con `catalogo_tipologias.py` — esto está bien hecho. Nota menor: cada `_param(..., default=X)` repite el valor real como *default* de fallback (p.ej. línea 40: `_param('D1-ancho-emparejamiento', 'tol_min_m', 0.08)`); hoy no es un bug (el catálogo siempre resuelve al valor real), pero es una segunda copia silenciosa del número — si algún día se borra la entrada del catálogo, el código no fallaría fuerte, usaría el default viejo sin avisar.

**Inconsistencias vs. documentación:** ninguna otra encontrada dentro del archivo mismo — está inusualmente bien documentado y autoconsciente de su propia historia de bugs (con fechas y casos reales citados).

**Bugs reales:** no encontré casos límite no manejados ni excepciones tragadas — el módulo **no tiene ningún `try/except`** (verificado por grep, 0 resultados), consistente con su filosofía explícita de "nunca fallar en silencio" (`_validar_mpx`, líneas 54-67, hace fail-fast explícito si `mpx` es inválido).

**Código muerto:** `diagnosticar_candidatos` (línea 543) está auto-declarado como "DIAGNOSTICO (no se usa en produccion)" en su propio docstring — no es un hallazgo nuevo, lo dice el código; confirmé además que SÍ se usa activamente en otras celdas del repo (ver hallazgo arquitectónico más abajo), así que no es código muerto real, es una herramienta de diagnóstico deliberada. `relleno_solido_de_contexto` (línea 1122) tampoco se llama desde ningún otro archivo de esta carpeta, pero confirmé que se usa en `Fase 2/Desarrollos/Test/*_celda4_funciones*.py` — tampoco es dead code real, solo no se usa desde `Herramientas_CubiCasa5k/`.

## 2. `catalogo_tipologias.py` (501 líneas)

**Qué hace:** Diccionario `TIPOLOGIAS` (una entrada por tipología de Convenciones_CAD.md sección D) con criterio, parámetros reales, estado de implementación (`implementado`/`parcial`/`pendiente`) y dónde vive el código. Función `parametro()` como único punto de lectura de valores paramétricos, y `resumen_estado()` para conteo agregado.

**Hardcodeos:** ninguno problemático — este archivo **es** la fuente de verdad paramétrica; todos sus valores (`tol_min_m: 0.08`, `tol_max_m: 0.9`, etc.) son los valores reales de negocio por diseño, no "hardcodeos" en el sentido de duplicación indebida.

**Inconsistencias vs. documentación:** el campo `"usa_en"` de `D1-D3-ventana-lineas-centrales` (línea 121) y `D3-ventana-reconstruccion-por-jamba` (línea 147) afirman que el resultado llega a "Celda 4" exportando a `ventanas_simples_por_linea_central` / `ventanas_reconstruidas_por_jamba`, y `D3-ventana-corta-muro` (línea 180) afirma estar wireado en "Celda 4 (post fusion...)". **Verifiqué por grep sobre el texto completo de `_celda4_actual.py`** (el archivo que la consigna identifica como "la celda que corre en Colab") que **ninguno** de esos nombres de campo, ni `muros_geo`, ni ninguna función de `cuerpo_cerrado.py`, aparece ahí — ver hallazgo arquitectónico grande más abajo (§5). El catálogo no se equivoca en general (esas funciones sí existen y sí se usan en otra parte del repo), pero su referencia genérica a "Celda 4" no corresponde al archivo identificado como el espejo local de esa celda.

**Bugs:** ninguno (es un diccionario de datos + 2 funciones triviales de lectura, sin lógica de negocio que pueda fallar).

**Código muerto:** `resumen_estado()` (línea 494) — confirmé por grep en todo el repo que no se llama desde ningún `.py` ni `.mjs`; función utilitaria nunca invocada.

## 3. `_celda4_actual.py` (1279 líneas) — comparación con `Fase 2/reglas_normativas.py`

**Qué hace:** Espejo local (para poder correr/testear sin Colab) de la celda que: llama a Claude Vision con el plano (imagen original + versión CLAHE/nítida), extrae texto y trazos vectoriales del PDF (`extraer_datos_vectoriales`, con su propia heurística de conectividad+span para proteger "muro" — **no usa `cuerpo_cerrado.py`**), limpia el raster con OpenCV, detecta recintos por componentes conexas, cruza nombre semántico (Claude) con geometría (OpenCV), y aplica `OGUC_REGLAS` (área/ancho mínimo, pendiente de rampa, círculo de giro, cuadro de superficies declarado vs. medido).

**Hallazgo central — 3 valores desincronizados de `reglas_normativas.py`, los 3 dentro del mismo bloque de reglas de elemento/rampa:**

1. **`_celda4_actual.py:138`** — `'puerta_ancho_libre': (None, 0.80, 'OGUC Art. 4.1.7 N°6 — ancho libre minimo 0,80 m (accesibilidad universal)')`. `reglas_normativas.py:191` corrigió este mismo valor a **0,90 m**, cita **N°4** ("CORREGIDO 2026-09-21... el valor anterior (0.80 m, citado como 'N°6') estaba MAL"), y separó el caso de 0,80 m en una entrada nueva `puerta_ancho_libre_bano_accesible` (`reglas_normativas.py:199`, específica para puerta de baño accesible). `_celda4_actual.py` sigue con el valor viejo y la cita vieja, pese a que el propio comentario de líneas 126-137 del archivo dice textualmente: "Si se corrige un valor, corregirlo PRIMERO en Fase 2/reglas_normativas.py y despues copiarlo aca... nunca al reves" — la regla de disciplina existe pero no se aplicó tras la corrección del 21-sep.
2. **`_celda4_actual.py:1168`** — `max_pendiente = round(12.8 - 0.5333 * desarrollo, 2)` usa la constante truncada `0.5333`. `reglas_normativas.py:382` usa la fracción exacta `(4.0 / 7.5)`, con hallazgo documentado en sus propias líneas 366-374: verificado empíricamente que hay 133 puntos en el rango 1,5-9,0 m donde el resultado redondeado difiere entre la constante truncada y la fracción exacta (0,01 punto porcentual, suficiente para cambiar un cumple/no-cumple en un caso límite). El propio comentario de `_celda4_actual.py:1140-1147` ya anticipa esto ("reglas_normativas.py es la fuente de verdad para cualquier cambio futuro al umbral/formula") pero no se aplicó la corrección.
3. **`_celda4_actual.py:1161-1162`** — `if desarrollo is None: max_pendiente = 8.0` — cuando falta el dato de desarrollo de la rampa, asume en silencio el máximo más estricto (8%) en vez de marcar "no evaluable". `reglas_normativas.py:376-377` (`pendiente_maxima_rampa_pct`) corrige esto devolviendo `None` explícito. Este es exactamente el patrón "dato ausente tratado como si aplicara una regla" — y está **auto-documentado como gap conocido y no corregido** en `_celda4_actual.py:1149-1160` ("DISCREPANCIA REAL detectada al migrar (no corregida aca hoy...)").

No encontré desync en `pasillo`/`escalera`/`rampa` (ancho) ni en `circulo_giro` (1.50 m, línea 1196, coincide con `reglas_normativas.py:224`) — esos sí están al día.

**Otros hardcodeos:** `UMBRAL_MURO_M = 1.5` (línea 316), `TOL_MURO_PX = 35` (línea 360, con nota propia de que subió de 12 a 35 tras una regresión real), `UMBRAL_AREA_SOSPECHOSA_M2 = 60` (línea 969), `UMBRAL_DASH_M = 1.0` / `TOL_DASH_GAP_PX = 40` / `TOL_DASH_ANGULO_DEG = 5` (líneas 500-502), `RADIO_ACHURADO_PX = 60` / `MIN_SEGMENTOS_ACHURADO = 5` / `MIN_EXTENSION_PERPENDICULAR_PX = 80` (líneas 413-416, con el fix de achurado **desactivado** — `ACHURADO_DESPROTEGER_ACTIVO = False`, línea 480, tras confirmarse en Colab que causaba una regresión catastrófica real). Todos estos están documentados con su propia historia de calibración empírica (no son hardcodeos "silenciosos"), pero son íntegramente locales a este archivo, sin paso por `catalogo_tipologias.py` — son parte de la heurística de conectividad propia de este pipeline "generador", independiente del motor de `cuerpo_cerrado.py`.

**Bugs reales — excepciones tragadas en silencio (confirmado, no auto-documentado como tal):**
- **`_celda4_actual.py:853-854`** — `except:\n    pass` (except bare) dentro del parseo de chunks SSE de la respuesta de Claude Vision en el loop principal. Si un chunk JSON individual falla al parsear, se descarta sin ningún log — el único rastro que queda es que más adelante, si `raw_text` terminó corrupto, el regex de extracción de JSON (línea 856) puede fallar y cae al mensaje genérico "Claude: sin JSON en respuesta" (línea 863-864 aprox.), sin indicar que la causa real fue un chunk SSE descartado.
- **`_celda4_actual.py:683-684`** — mismo patrón (`except Exception: pass`) dentro de `detectar_figuras_lamina`.

**Código muerto:**
- **`_celda4_actual.py:7`** — `from datetime import datetime`: confirmé por grep que `datetime` no se usa en ningún otro punto del archivo (1279 líneas) — import sin uso.
- `detectar_figuras_lamina` (línea 591) está auto-declarado "OPCIONAL, no se llama automaticamente desde el loop principal todavia" (línea 593) — no es un hallazgo nuevo, lo dice el propio docstring.

## 4. Suite de tests

- **`test_cuerpo_cerrado.py`** (263 líneas): 9 grupos de casos (CASO 1 a CASO 9, cada uno con sub-casos), **24 aserciones individuales** en total. Nota: el docstring del archivo (línea 3) dice "mismos 4 casos de regresion, con sub-casos" — quedó desactualizado, en realidad son 9 grupos (crecieron incrementalmente 23 a 26-ago según los propios comentarios fechados) — inconsistencia menor de documentación, no de lógica.
- **`test_cuerpo_cerrado_properties.py`** (206 líneas, usa `hypothesis`): **5 funciones de test**, cada una probando una invariante distinta:
  1. `test_dividir_en_muros_es_una_particion` — `_dividir_en_muros_por_union` particiona exactamente (sin perder ni duplicar índices).
  2. `test_ancho_por_emparejamiento_respeta_rango_de_tolerancia` — todo `anchoPx` devuelto cae en `[tol_min_px, tol_max_px]`.
  3. `test_ancho_heredado_no_fabrica_valores` — un conector nunca "inventa" un ancho: el heredado siempre coincide con un `anchoPx` real de algún segmento del contexto.
  4. `test_cuerpo_cerrado_fusiona_respeta_su_propio_contrato` — si fusiona, `anchoA`/`anchoB` son ambos `None` (contacto directo) o ambos no-`None`; si no fusiona, siempre hay `motivo`.
  5. `test_no_crashea_con_geometria_arbitraria` — robustez/fuzzing ligero sobre las 3 funciones principales.
- **`test_cuerpo_cerrado_golden.py`** (112 líneas): **3 tests** marcados `@pytest.mark.golden` (uno por proyecto real: PdV, Campo Lindo, Beauchef) que corren el pipeline completo vía `_tmp_generar_json_final.py` (subprocess) y comparan contra baselines numéricos hardcodeados (`BASELINE_PDV = [38, 37]`, etc.) — **excluidos por defecto** de cualquier corrida normal.
- **`conftest.py`** (8 líneas): solo agrega el directorio del script a `sys.path`.
- **`pytest.ini`** (13 líneas): `addopts = --ignore=test_threshold_bajo.py -m "not golden"` — excluye explícitamente `test_threshold_bajo.py` (pese al nombre "no es un test real", requiere torch/cuda) y excluye los tests `golden` por defecto.
- **Skip/xfail/TODO/FIXME:** **cero encontrados** en toda la suite — suite limpia en ese sentido.
- **`_test_reconstruccion_ventanas.py`** (29 líneas): **no es recolectado por pytest** porque su nombre no matchea ni `test_*.py` ni `*_test.py` (empieza con guion bajo + "test_"). Es una validación real de `reconstruir_ventanas_por_jamba` contra un JSON real de Beauchef, pero queda fuera de la suite automatizada por un problema de nomenclatura del archivo.

## 5. Hallazgo arquitectónico mayor: `_celda4_actual.py` no integra `cuerpo_cerrado.py` — existe una segunda línea de "Celda 4" divergente fuera de esta carpeta

Verificado por grep sobre el texto completo (no solo lectura): `_celda4_actual.py` **no contiene ningún `import` ni llamada** a `cuerpo_cerrado_fusiona`, `ancho_por_emparejamiento`, `identificar_lineas_centrales`, `clasificar_no_muro`, `cortar_muros_por_ventanas`, `reconstruir_ventanas_por_jamba`, ni a `catalogo_tipologias.py`; tampoco produce en ningún punto las variables `muros_geo`, `puertas_geo`, `ventanas_simples_por_linea_central` ni `ventanas_reconstruidas_por_jamba` que `catalogo_tipologias.py` (líneas 121, 147, 180-181) describe como el destino de export de esas tipologías en "Celda 4". La única mención a "cuerpo_cerrado.py" en todo el archivo es un comentario de precaución en la línea 1159; `reglas_normativas` solo se menciona en comentarios (línea 1184), nunca se importa (consistente con la limitación de Colab documentada).

El propio docstring de `cuerpo_cerrado.py` (líneas 3-10) dice que reemplaza "el filtro duro de tipo/angulo/capa del generador (Paso 2 de extraer_datos_vectoriales)" — pero el "Paso 2" real que vive en `_celda4_actual.py:346-374` sigue usando su propio heurístico independiente de conectividad+span (`TOL_MURO_PX=35`), no tocado por `cuerpo_cerrado.py`.

Rastreando `diagnosticar_candidatos`/`relleno_solido_de_contexto` (funciones exclusivas de `cuerpo_cerrado.py`) con grep en todo el repo, encontré que **sí se importan y usan activamente**, pero en un árbol de archivos completamente distinto, fuera de `Herramientas_CubiCasa5k/`:
- `Fase 2/Desarrollos/Test/Celda 4 - copiar en Colab.py`
- `Fase 2/Desarrollos/Test/Beauchef/_celda4_funciones.py`, `_celda4_funciones_ORIGINAL.py`, `_celda4_funciones_PORTADO.py`
- `Fase 2/Desarrollos/Test/Isla de Pascua/_celda4_funciones.py`
- Decenas de notebooks fechados en `Fase 2/Desarrollos/Test/Versiones anteriores/*.ipynb`

Por fecha de modificación en git: `_celda4_actual.py` y `reglas_normativas.py` comparten el mismo commit (2026-09-20), mientras que `Celda 4 - copiar en Colab.py` es del 2026-08-29 (3 semanas más viejo) y tiene su **propia copia inline** de `OGUC_REGLAS` (confirmé que no importa `reglas_normativas`) — es decir, ese árbol probablemente arrastra el mismo tipo de desync de `puerta_ancho_libre`/pendiente que se encontró en `_celda4_actual.py`, pero no se leyó completo (está fuera de la carpeta asignada al agente) y no se puede confirmar sin leerlo.

**No se pudo verificar** cuál de los dos árboles es el que realmente corre hoy en el notebook vivo de Colab: se revisó la carpeta externa `Docs archicheck/Doc prueba/` (a la que apunta `aplicar_fix_celda4.mjs` como destino de deploy) y **no hay ningún `.ipynb` ahí actualmente** — el script fallaría si se corriera ahora mismo ("Se esperaba exactamente 1 notebook vivo... se encontraron 0"), lo cual es coherente con que el notebook esté actualmente abierto/sincronizado solo en Google Colab, no en el filesystem local. En síntesis: **hay dos líneas paralelas de "Celda 4"** (una con el motor `cuerpo_cerrado.py` de fusión de muros pero con `OGUC_REGLAS` propia sin importar de `reglas_normativas.py`, y otra —`_celda4_actual.py`— con `OGUC_REGLAS` casi sincronizada pero sin el motor de fusión geométrica), y no hay evidencia dentro de los archivos leídos de que se hayan unificado. **SIN VERIFICAR.**

## 6. Archivos `.py` adicionales (no `_tmp_`/`test_`) leídos completos

De 29 archivos `.py` totales en la carpeta, 4 ya estaban en la lista asignada, 4 empiezan con `test_` y **11 no encajan en ningún patrón** (`_tmp_`/`test_`). De esos 11, `correr_revisiones.py` ya lo había leído el usuario; los 6 restantes que no estaban cubiertos, se leyeron completos:

- **`_celda4_nueva.py`** (529 líneas) y **`_celda4_extraida.py`** (440 líneas): son snapshots **antiguos** (23-jul-2026), no versiones nuevas pese al nombre — `_celda4_extraida.py` es la extracción cruda de la celda de ese momento, `_celda4_nueva.py` es casi idéntico salvo que agrega manejo de `lineas_discontinuas` (detección de trazos punteados vía `path['dashes']`, método luego abandonado y reemplazado por la heurística geométrica de colinealidad+gap que usa `_celda4_actual.py`). Ambos conservan el `OGUC_REGLAS` más viejo y ya reconocido como erróneo (`'pasillo': (None, 1.20, 'Art. 4.2.2 OGUC...')`, `'escalera': (None, 1.20, 'Art. 4.2.4 OGUC...')`, `'rampa': (None, 1.20, 'Art. 4.1.7 OGUC + DS 50/2015')` — exactamente las citas que el comentario de `_celda4_actual.py:11-19` documenta como cruzadas/incorrectas). **Riesgo operativo real:** `aplicar_fix_celda4.mjs:26` usa `_celda4_nueva.py` como **argumento por defecto** (`process.argv[2] || ".../_celda4_nueva.py"`) para pegar sobre el notebook vivo de Colab — si se corre sin argumento explícito hoy, sobreescribiría el notebook vivo con código de más de un mes de antigüedad, con reglas OGUC ya identificadas como incorrectas y sin ninguna de las mejoras posteriores (vectorial con líneas discontinuas separadas, cruce de cuadro de superficies, círculo de giro, pendiente de rampa real, etc.).
- **`_test_reconstruccion_ventanas.py`** (29 líneas): validación real de `reconstruir_ventanas_por_jamba` contra el JSON real de Beauchef (`archicheck_geometrico_beauchef_30ago_0356.json`, ruta relativa hardcodeada línea 16) — no modifica nada, solo imprime resultado. Como se señaló arriba, no es recolectado por pytest por su nombre.
- **`celda_test_cubicasa5k.py`** (165 líneas), **`extraer_posiciones_threshold03.py`** (73 líneas) y **`parche_scipy_mode.py`** (24 líneas): rama exploratoria completa para evaluar el modelo pre-entrenado CubiCasa5K (segmentación ML) como alternativa/complemento al pipeline vectorial — requieren GPU/Colab, no producción. `extraer_posiciones_threshold03.py:15` fija `THRESHOLD = 0.3` (umbral de confianza del modelo) como constante hardcodeada, y documenta un bug real ya corregido ahí mismo (línea 5-8: índice de clase `numpy.int64` vs `int` de Python causaba que puertas reales cayeran en un bucket erróneo `"clase_2"` — corregido forzando `int()`). `parche_scipy_mode.py` es una "v2 más robusta" (parche en memoria) de un fix que `celda_test_cubicasa5k.py:33-50` ya aplicaba de otra forma (edición del archivo del repo CubiCasa5K en disco vía regex) — dos mecanismos distintos para el mismo problema de compatibilidad de `scipy.stats.mode()`, coexistiendo sin que uno reemplace formalmente al otro (evidencia de iteración en una rama que parece abandonada/aparcada a favor del pipeline vectorial).

## 7. Categorización del resto de la carpeta (169 archivos)

Conteo por extensión: **79 `.mjs`**, **29 `.py`**, **27 `.png`**, **15 `.json`**, **10 `.csv`**, **8 `.txt`**, **1 `.ini`**. De los 169: **68 empiezan con `_tmp_`** (scripts de diagnóstico puntual) y **4 empiezan con `test_`**. Subcarpetas de salida/output no leídas en detalle (logs/artefactos, no código): `_codex_reviews/` (24 archivos), `_consultas/` (12), `_deepseek_reviews/` (18), `_perf_reviews/` (2), `_tmp_diag_out/` (61). `.hypothesis/`, `.pytest_cache/`, `__pycache__/` son cachés automáticos.

**Los 29 `.mjs` que no empiezan con `_tmp_`** — se revisó el encabezado/docstring de los 29 (no el archivo completo, dado el volumen) y **ninguno es código de producción del pipeline**; todos caen en 3 categorías, consistentes con lo que documentan sus propios comentarios:
- **Generación/verificación de ground truth**: `comparar_posiciones.mjs`, `diff_vs_marcas_usuario.mjs`, `generar_gt_final.mjs`, `generar_gt_v3.mjs`, `reconstruir_ground_truth.mjs`, `remedicion_v3.mjs`, `verificar_gt_final.mjs`, `investigacion_profunda.mjs`, `extraer_curvas_puertas.mjs`.
- **Visualización/diagnóstico**: `generar_grilla.mjs`, `graficar_comparacion.mjs`, `marcar_detecciones_sistema.mjs`, `recortar_zoom_cubicasa.mjs`, `visualizar_combinado.mjs`, `visualizar_muros.mjs`, `visualizar_recintos_bbox.mjs`, `visualizar_trazos_borrados.mjs`, `zoom_grilla_generico.mjs`, `detectar_etiquetas_rojas.mjs`, `leer_xlsx_manual.mjs`.
- **Tooling de revisión con IA externa** (ligados a "Convenciones_CAD.md D.11"): `consultar_bug_cadena_conectores.mjs`, `consultar_bug_conector_ancho_implausible.mjs`, `consultar_gap_export_ventanas.mjs`, `consultar_reconstruccion_ventanas_jamba.mjs`, `revisar_con_codex.mjs`, `revisar_con_deepseek.mjs`, `revisar_hardcode_con_deepseek.mjs`, `revisar_performance_beauchef.mjs`, `validar_fixes_con_deepseek.mjs` — todos requieren credenciales locales (`.env.deepseek.local`/`.env.openai.local`, gitignored) y son de disparo manual.
- **`aplicar_fix_celda4.mjs`** (caso aparte): es la herramienta de **deploy** que mueve el notebook vivo de `Docs archicheck/Doc prueba/` a un backup timestamped y pega una fuente `.py` nueva dentro de la celda `id="cell-4"` del notebook — ver riesgo de su default apuntando a `_celda4_nueva.py` (desactualizado), sección 6.

## 8. Lo que no se pudo verificar (SIN VERIFICAR)

- El contenido completo de `Fase 2/Desarrollos/Test/Celda 4 - copiar en Colab.py` y sus variantes `_celda4_funciones*.py` (fuera del alcance de la carpeta asignada) — solo se confirmó por grep que importan `cuerpo_cerrado.py` y que `Celda 4 - copiar en Colab.py` define su propio `OGUC_REGLAS` sin importar `reglas_normativas.py`, pero no se leyó ese diccionario completo para confirmar si tiene el mismo desync de `puerta_ancho_libre`/pendiente que `_celda4_actual.py`.
- Cuál de las dos líneas de "Celda 4" es la que efectivamente corre hoy en el Colab del usuario — no hay notebook vivo en `Docs archicheck/Doc prueba/` en este momento para confirmarlo.
- El contenido de las 27 imágenes `.png`, 15 `.json`, 10 `.csv` y 8 `.txt` de la carpeta — no se abrieron (son datos/artefactos de ground truth y verificación visual, consistente con lo que describen los scripts que los generan, no código).
